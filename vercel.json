{
  "version": 2,
  "builds": [{
    "src": "app.py",
    "use": "@vercel/python",
    "config": {
      "pythonVersion": "3.10",
      "installCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt"
    }
  }],
  "routes": [{
    "src": "/(.*)",
    "dest": "app.py"
  }]
}